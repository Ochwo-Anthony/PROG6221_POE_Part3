<Application x:Class="CyberSecurity_ChatBot.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CyberSecurity_ChatBot"
             StartupUri="IntroWindow.xaml">
    <Application.Resources>
        
        <SolidColorBrush x:Key="AccentColor" Color="#3B82F6" />
        <SolidColorBrush x:Key="PrimaryColor" Color="#0F172A" />
        <SolidColorBrush x:Key="HoverAccentColor" Color="#2563EB" />

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentColor}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HoverAccentColor}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="16" />
        </Style>

        <Style TargetType="Window">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}" />
        </Style>

    </Application.Resources>
</Application>
